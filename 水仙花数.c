#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);

    int start = 1;
    for (int i = 1; i < n; i++)
    {
        start *= 10;
    }
    int end = start - 1;

    for (int num = start; num < start; num++)
    {
    }

    return 0;
}